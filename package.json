{
  "name": "agricola-multiplayer",
  "version": "1.0.0",
  "private": true,
  "description": "Express and Socket.IO backend for an Agricola multiplayer board game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
